# LoomHash Project Status

## 2026-09-17 21:11:41 +05:30 — Project context initialized

- Saved the supplied architecture and agent directives verbatim in system.md.
- Workspace was empty before initialization; no application components have been implemented.
- No dependencies have been installed or added.
- Implementation scope remains pending user selection. Clarify ambiguous implementation details, API routes, and database schemas before implementing them, as required by system.md.
- Security, biometric accuracy, deepfake resistance, anonymization, and regulatory compliance claims in the supplied specification have not been validated.

## Next step

Review docs/open-decisions.md and resolve feature extraction, projection, and security-scope decisions before implementation.

## 2026-09-17 21:17:50 +05:30 — Initial documentation and requirements analysis completed

- Inspected the workspace and read system.md and STATUS.md; no pre-existing documentation folder or application code was present.
- Created docs/README.md, docs/requirements.md, docs/architecture.md, and docs/open-decisions.md.
- Mapped specified enrollment, authentication, and consent-revocation behavior to acceptance evidence and module responsibilities.
- Documented unresolved feature definitions, LSH determinism, ONNX integration, identity binding, memory constraints, replay resistance, and deletion lifecycle.
- Checked primary MediaPipe, ONNX Runtime, MeitY, and EDPB sources; linked references and distinguished analysis from specified requirements.
- Preserved system.md unchanged. No application code, dependencies, API routes, or database schemas were introduced.
- Verification: checked documentation links and the derived 51/52-bit matching boundary. Runtime tests are not applicable to this documentation-only change.

## 2026-09-17 — Documentation set expanded: memory, hallucination checks, use cases, per-module agent context

- Asked the user four scoping questions before writing: milestone target, agent-context file granularity, hallucination-check format, and live-tracking granularity. Answers: feasibility prototype, per-module agent files, checklist + automated script, and a dedicated verification-checklist.md alongside STATUS.md.
- Added docs/use-cases.md: actor-level UC-1 (enroll), UC-2 (authenticate), UC-3 (revoke) flows with prototype-scope vs. deferred edge cases.
- Added docs/memory-system.md documenting the file-based memory tiers (system.md, STATUS.md, open-decisions.md, verification-checklist.md) and the session start/end read-write protocol.
- Added docs/hallucination-checks.md operationalizing system.md's four immutable constraints and "Ask, Do Not Assume" rule into a pre-flight checklist.
- Added scripts/check_constraints.py, a heuristic grep-based scanner for forbidden vector-DB imports, server-side media-processing imports, raw-image-write patterns, and non-`int.bit_count()` popcount code. Ran it against the repo: 1 file scanned (itself), 0 hard violations, 3 expected self-referential warnings from its own pattern strings.
- Added docs/verification-checklist.md: one Pending row per requirement ID (ENR-01..REV-01), to be flipped to Verified only with linked evidence.
- Added docs/agents/{edge-extraction,cryptography,inference,storage,compliance,api-gateway}.md: per-module role, I/O contract, applicable immutable constraints, and explicitly undefined items not to be invented.
- Edited docs/open-decisions.md to add a "Resolved decisions" section recording the feasibility-prototype milestone choice and reclassifying D-01..D-10 as blocking vs. deferred under that scope.
- Updated docs/README.md's reading order and working rules to reference all new files.
- Verification: read every new/edited file back; ran the new script successfully. No application code, dependencies, API routes, or database schemas were introduced; system.md remains unchanged.

## 2026-09-17 — Guardrails and system checks wired up (git, hooks, CI, agent auto-load)

- Asked the user three scoping questions: whether to `git init` (yes), which guardrail layers to add (root CLAUDE.md, pre-commit hook, GitHub Actions CI, pyproject.toml Python pin — all four), and whether to add a doc-consistency doctor script beyond the constraint scanner (yes).
- Ran `git init` — this was previously not a git repository. No commit was made; files are left staged for the user to review and commit.
- Added scripts/check_docs.py: cross-checks that every requirement ID in docs/requirements.md appears in docs/verification-checklist.md, every D-xx decision ID referenced under docs/ is defined in docs/open-decisions.md, no verification-checklist.md row is marked Verified with an empty evidence cell, and all six docs/agents/*.md files exist. Ran it clean against the current docs.
- Added hooks/pre-commit (tracked) running both check_constraints.py and check_docs.py, and installed a shim at .git/hooks/pre-commit that execs the tracked copy so the logic stays versioned. Verified the installed hook runs and exits 0 against the current repo state.
- Added .github/workflows/guardrails.yml running both scripts on push/pull_request with Python 3.10.
- Added pyproject.toml declaring `requires-python = ">=3.10"` (constraint 2) and no dependencies.
- Added root CLAUDE.md so Claude Code auto-loads the session-start reading order, the four immutable constraints, the guardrail wiring, and the "Ask, Do Not Assume" working rules at the start of every session, rather than relying on being told to open docs/.
- Cross-linked the new guardrail files from docs/hallucination-checks.md (Enforcement section) and docs/memory-system.md (CLAUDE.md added to the memory-tier table).
- Verification: ran `python scripts/check_constraints.py` and `python scripts/check_docs.py` directly and via the installed pre-commit hook — all exit 0. No application code, dependencies (beyond the requires-python declaration), API routes, or database schemas were introduced; system.md remains unchanged.

## 2026-09-17 — Cryptography module: first implementation (D-03 resolved)

- Asked the user which module to start with; picked Cryptography as lowest-setup (pure Python, numpy already spec-approved, no new deps).
- Proposed a concrete resolution for D-03 (seeded LSH definition) before writing any code: random-hyperplane/SimHash projection, 256x128 iid N(0,1) matrix seeded via `numpy.random.default_rng(int.from_bytes(seed, "big"))`, sign-bit binarization (>=0 -> 1), MSB-first packing, float64, tagged `LSH_VERSION = 1`. User approved as proposed.
- Implemented loomhash/cryptography/lsh.py (`generate_seed`, `projection_matrix`, `project`) and loomhash/cryptography/matching.py (`hamming_distance`, `is_match`), satisfying constraint 1 (no vector-DB import) and constraint 2 (`int.bit_count()`, not a popcount fallback).
- Added tests/test_cryptography/{test_lsh,test_matching}.py using stdlib `unittest` (no new test-framework dependency added without consent): seed/vector determinism and validation with synthetic (non-biometric) vectors, plus the exact AUT-03/AUT-04 boundary (51/256 = 0.19921875 accepts, 52/256 = 0.203125 rejects). One test (`test_different_vector_gives_different_hash`) initially failed because a uniform `+1.0` vector shift happened not to cross zero on any of the 256 projected components for the fixed test seed — not an implementation bug, but a weak test; fixed by asserting against a negated vector instead, which is guaranteed (up to measure-zero exact-zero ties) to flip virtually every sign bit. All 14 tests now pass.
- Updated pyproject.toml: added `numpy>=1.24` as a dependency (already mandated by system.md section 3, not a new agent-introduced dependency) and setuptools packaging config so `loomhash` is installable.
- Updated docs/open-decisions.md: added D-03's resolution to the "Resolved decisions" table with the exact parameters and a pointer to the code; struck through the original D-03 row in the blocking table rather than deleting it.
- Updated docs/verification-checklist.md: ENR-04, AUT-03, and AUT-04 moved from Pending to Verified (algorithmic/matching-logic scope only — explicitly noted that biometric-accuracy evaluation and the actual HTTP 200/401 response mapping are still separate, unverified concerns), each with a link to the specific test file and cases.
- Updated docs/agents/cryptography.md to point at the implementation and warn that changing any LSH parameter now requires bumping `LSH_VERSION`.
- Verification: ran `python -m unittest discover -s tests -t .` (14/14 pass), `python scripts/check_constraints.py` (0 hard violations), and `python scripts/check_docs.py` (consistent) — all after these changes.

## 2026-09-17 — Storage module: second implementation (D-10, D-05 minimal, D-06 resolved)

- User picked Storage as the next module over Edge Extraction (lower setup cost, unblocks a full enroll->store->authenticate loop using the already-built cryptography module).
- Before writing code, checked for local infra: no psycopg/psycopg2/redis installed, and neither PostgreSQL (5432) nor Redis (6379) reachable on localhost in this environment.
- Asked three scoping questions and got explicit approval for each: (1) schema/cache design — Postgres `enrollments(user_id, seed, loom_hash, lsh_version, created_at, updated_at)` as source of truth, Redis as a write-through cache, upsert-on-re-enroll; (2) revocation failure semantics — delete() must report failure (not best-effort success) if either store fails, so a lingering cached seed can't defeat crypto-shredding; (3) dependency approach — add `psycopg[binary]` and `redis-py` now as new approved dependencies, even though neither can be integration-tested here without live services.
- Added `hash_to_bytes`/`hash_from_bytes` to loomhash/cryptography/lsh.py (32-byte big-endian serialization matching the existing MSB-first bit packing) so loom_hash can be stored as BYTEA/Redis-hash-field bytes; round-trip tested in test_lsh.py.
- Implemented loomhash/storage/: `backend.py` (StorageBackend ABC + EnrollmentRecord contract), `memory.py` (InMemoryStorageBackend, a non-durable fake for tests), `postgres.py` (PostgresStorageBackend using psycopg, upsert via `ON CONFLICT DO UPDATE`), `redis_cache.py` (RedisEnrollmentCache using redis-py, write-through, never authoritative), `combined.py` (PostgresRedisStorageBackend wiring both together; `delete()` returns `postgres_ok and cache_ok`). `loomhash/storage/__init__.py` deliberately does not import the Postgres/Redis modules at package level, so `import loomhash.storage` never requires those drivers to be importable.
- Installed psycopg[binary] and redis-py via pip in this environment and added both to pyproject.toml's dependencies with a comment explaining why (spec-mandated tech, explicitly approved, untested against live services).
- Added tests/test_storage/test_contract.py: a shared `StorageBackendContractTests` mixin (round-trip, upsert-on-re-enroll, per-user isolation, delete + idempotent delete, delete-then-re-enroll), run against `InMemoryStorageBackend`. Added test_adapters_construct.py: import/construction-only smoke tests for the Postgres/Redis adapters, explicitly documented as NOT exercising real database behavior since no live instance exists here.
- Updated docs/open-decisions.md: added D-10, D-05 (minimal/prototype-scope only), and D-06 to "Resolved decisions" with full parameters; struck through their rows in the blocking table; reclassified the summary paragraph so only D-01/D-02 (Edge Extraction) remain blocking among the non-deferred items.
- Updated docs/verification-checklist.md: ENR-05, AUT-02, and REV-01 moved to "Verified (contract only)", each evidence note explicitly flagging that the real Postgres/Redis adapters remain unverified against live services, and that REV-01's partial-failure path (one store succeeds, one fails) has no test at all yet because the in-memory fake has no way to fail.
- Updated docs/agents/storage.md to point at the implementation and flag the live-service testing gap prominently so a future agent doesn't mistake "contract verified" for "adapters proven against real Postgres/Redis."
- Verification: ran `python -m unittest discover -s tests -t .` (27/27 pass), `python scripts/check_constraints.py` (0 hard violations), and `python scripts/check_docs.py` (consistent).

## 2026-09-17 — API Gateway module: third implementation (D-10 routes resolved)

- Proposed and got approval for two decisions before writing code: (1) route/schema design — `POST /v1/enroll`, `POST /v1/authenticate` (no distance value in the response, to prevent hill-climbing the threshold), `DELETE /v1/users/{user_id}` (500 on unconfirmed revocation); (2) unknown/never-enrolled user_id on authenticate gets the same 401 as a failed match, not a distinct 404, to avoid letting the endpoint be used for user-id enumeration.
- Implemented loomhash/api/: `schemas.py` (Pydantic request/response models, `extra="forbid"` so an unexpected field like `image` is rejected rather than silently dropped), `app.py` (`create_app(storage=None)` wiring the three routes to the cryptography and storage modules, defaulting to `InMemoryStorageBackend`), `__main__.py` (manual local run via `python -m loomhash.api`).
- Installed/added fastapi, uvicorn (already spec-mandated by system.md) and httpx (test-only, required by `fastapi.testclient.TestClient`) to pyproject.toml.
- Hit a real compatibility issue: the already-installed fastapi 0.110 bundled starlette 0.36, whose `TestClient` was built against an httpx `app=` constructor argument that httpx 0.28 (the version installed) removed — `TestClient(...)` raised `TypeError: Client.__init__() got an unexpected keyword argument 'app'`. Fixed by upgrading to fastapi>=0.115 (pulls a newer starlette compatible with current httpx). Not a code bug; documented in pyproject.toml's dependency comment so a future agent doesn't reintroduce the old floor.
- Added tests/test_api/test_routes.py: full enroll -> authenticate -> revoke loop over real HTTP requests (FastAPI TestClient, in-memory backend) — same-vector success, different-vector (negated) failure, unknown-user 401 (not 404), revoke-then-authenticate failure, idempotent revoke of an unknown user, wrong-length-vector 422, empty-user_id 422, unexpected-extra-field 422, and re-enrollment issuing a fresh seed that invalidates the old vector. 38/38 tests pass across the whole repo.
- Updated docs/open-decisions.md: added the route/schema resolution as the remainder of D-10; updated the summary paragraph so D-01/D-02 (Edge Extraction) are the only requirement-blocking items left among the non-deferred set.
- Updated docs/verification-checklist.md: ENR-03 verified (server-side enforcement only — `extra="forbid"` proves the server can't be handed more than a vector, not that the not-yet-built client never tries); AUT-04 and REV-01 upgraded from algorithmic/contract-only to "Verified end-to-end (in-memory backend)", explicitly still noting the real Postgres/Redis backend and the partial-failure revocation path remain untested against live services.
- Updated docs/agents/api-gateway.md to point at the implementation and flag two things not to "fix": no distance field in the authenticate response, and no request/response logging (constraint 3 applies here too).
- Verification: ran `python -m unittest discover -s tests -t .` (38/38 pass), `python scripts/check_constraints.py` (0 hard violations), and `python scripts/check_docs.py` (consistent).

## 2026-09-17 — Compliance module: fourth implementation (boundary fix, no new D-item)

- User asked what step needs the ONNX file; answered that D-04 is still unresolved and neither pipeline calls it — offered three candidate purposes (liveness classifier, landmark->vector regressor, frame-quality scorer) and asked which was intended. No answer given; user asked to move to the next module instead, so Inference stays untouched/blocked.
- Noted that REV-01 was, until now, implemented directly inside the API Gateway's DELETE route (calling storage.delete() with no separate authorization step) — blurring the Compliance/API Gateway boundary architecture.md defines, even though no open decision explicitly blocked building Compliance separately. User picked Compliance as the next module over Edge Extraction to fix this.
- Implemented loomhash/compliance/revocation.py: `revoke(storage, user_id)` calls `is_authorized(user_id)` (currently a placeholder that always returns True, matching D-05's already-approved minimal/trusted-caller scope) then delegates to `storage.delete()`, raising `RevocationDenied` if not authorized. Documented as the seam where real authorization gets added later without touching the API route again.
- Rewired loomhash/api/app.py's DELETE /v1/users/{user_id} to call `loomhash.compliance.revoke()` instead of `storage.delete()` directly; added a 403 response mapping for `RevocationDenied`.
- Added tests/test_compliance/test_revocation.py, including a stub `AlwaysFailsToDeleteBackend` (delete() always returns False) to finally test the partial-failure path that was previously flagged in verification-checklist.md as having no test coverage at all (in-memory or real). Added matching API-level tests (`test_revoke_returns_403_when_not_authorized`, `test_revoke_returns_500_when_storage_reports_incomplete_deletion`) confirming the route maps both failure modes correctly.
- Noted (not acted on): a new `StarletteDeprecationWarning` appeared during test runs — "Using httpx with starlette.testclient is deprecated; install httpx2 instead" — from the fastapi/starlette upgrade made in the API Gateway session. Tests still pass; flagged here so a future agent isn't surprised and can decide whether to migrate to httpx2 when that dependency is more mature.
- Updated docs/verification-checklist.md's REV-01 row: partial-failure path is now tested via the stub backend, with an explicit caveat that this proves the API/Compliance call chain reacts correctly to a reported failure, not that the real Postgres/Redis adapter detects a partial outage correctly.
- Updated docs/agents/compliance.md and docs/agents/api-gateway.md to describe the new call chain and explicitly warn that `is_authorized`'s current always-True behavior is a deliberate placeholder, not a real authorization decision.
- Verification: ran `python -m unittest discover -s tests -t .` (44/44 pass), `python scripts/check_constraints.py` (0 hard violations), and `python scripts/check_docs.py` (consistent).

## 2026-09-17 — Edge Extraction module: fifth implementation (D-01, D-02 resolved)

- Before proposing D-01/D-02, fetched MediaPipe's actual Face Landmarker web/JS docs (WebFetch) rather than relying on memory for landmark counts/API shape, since D-01 explicitly warns against inventing landmark index semantics. Confirmed: `@mediapipe/tasks-vision` npm package, 478 landmarks in the docs' output example, `facialTransformationMatrixes` field exists — but could not confirm the JS-side exact camelCase field names or the transformation-matrix's internal shape (rows/columns/data vs flat array) from available pages.
- Proposed and got approval for three things: (1) D-01 — MediaPipe alignment via `facialTransformationMatrixes`, centroid/RMS-radius normalization, 128 deterministic evenly-spaced landmark indices (avoids hand-picking anatomical indices from memory); (2) D-02 — per-frame vector extraction then averaging (not raw-landmark averaging), >=8-of-12 valid frames required; (3) scope/testing — user said to build a testable core plus "a simple html js based applet to test the working," explicitly overriding my two proposed options (Next.js scaffold now, or testable-core-with-no-UI) with a third approach.
- While implementing D-01, discovered that centroid-relative distance (the chosen feature) is mathematically invariant to ANY rotation, translation, or uniform scale applied to the whole landmark set — proved this algebraically, then confirmed it with actual rotation/translation/scale tests, not just an assertion. This makes explicitly applying MediaPipe's `facialTransformationMatrixes` both redundant (for a pure rigid transform) and an unnecessary dependency on an API shape that couldn't be confirmed from docs. Dropped that step, documented the revision (not silently changed) in docs/open-decisions.md's D-01 row and a dedicated note, since it alters what was just approved a few messages earlier.
- Implemented edge/feature_extraction.mjs (pure math: centroid, RMS-radius normalization, 128-index selection, FEATURE_VERSION=1) and edge/capture.mjs (12-frame enrollment averaging with an 8-frame minimum via InsufficientFramesError, single-frame authentication using the identical per-frame pipeline). Zero npm dependencies — used Node's built-in test runner (`node --test`) instead of adding Jest/Vitest, since Node 20.11.1 was already available and this needed no new dependency approval.
- Added edge/feature_extraction.test.mjs and edge/capture.test.mjs (15 tests total, all passing) using deterministic synthetic landmark data (no randomness): index-selection sanity, wrong-input rejection, determinism, and explicit proofs of translation/rotation/scale/combined invariance. One test initially failed on floating-point rounding (summing 12 identical floats then dividing isn't bit-exact with computing once) — fixed with a tolerance comparison, not a code change.
- Added edge/mediapipe_adapter.mjs as the sole boundary where MediaPipe's real JS API shape matters, explicitly flagged as unverified against a live browser, defensively handling both a 468-length and >468-length landmark array (slicing to the first 468) so an uncertain assumption about iris-landmark ordering degrades gracefully rather than silently producing wrong data.
- Built edge/applet/ (index.html + main.mjs): a manual-test page loading `@mediapipe/tasks-vision` from a CDN (no npm install), wiring camera capture through the tested core to the live LoomHash API (enroll/authenticate/revoke buttons), logging every raw MediaPipe result to the console specifically so the adapter's unverified assumptions can be checked against ground truth by whoever actually runs it in a browser.
- Added CORS support to loomhash/api/app.py: `create_app(storage=None, *, enable_dev_cors=False)` — defaults to no CORS at all (tests and any real deployment stay unaffected), and `python -m loomhash.api` now passes `enable_dev_cors=True` so the applet (served from a different local origin) can actually call the API. Added tests confirming CORS is off by default and correctly scoped to localhost/127.0.0.1 origins when enabled.
- Wrote edge/README.md with exact run instructions (`node --test edge/` for unit tests; `python -m loomhash.api` + `python -m http.server` in `edge/applet/` for the manual test) and an explicit "what's not verified yet" section.
- Updated docs/open-decisions.md (D-01 and D-02 resolutions plus the mid-implementation revision note, blocking-table strikethroughs), docs/verification-checklist.md (ENR-01 marked "implemented, untested in a real browser"; ENR-02 and AUT-01 marked "Verified (algorithmic only)" with the D-02 reinterpretation flagged), and docs/agents/edge-extraction.md.
- Noted (not acted on): `scripts/check_constraints.py` only scans `*.py` files, so it does not cover the new `edge/*.mjs` code at all. No actual constraint-3/4 violation exists in the new JS (no image persistence, no server-side media processing — MediaPipe running client-side in `edge/` is exactly where the spec requires it), but the automated scanner's coverage gap is real; expanding it to JS is a reasonable future improvement, not done here since it wasn't part of the request.
- Verification: ran `python -m unittest discover -s tests -t .` (46/46 pass, includes new CORS tests), `node --test edge/` (15/15 pass), `python scripts/check_constraints.py` (0 hard violations), and `python scripts/check_docs.py` (consistent).

## 2026-09-18 — Docker infra for real Postgres/Redis on the user's VPS

- User wants to run real Postgres/Redis via Docker on their own VPS (this session has no access to it). Asked two questions: whether to SSH in directly or generate files to run manually (user picked: generate files), and network exposure (user picked: bind to localhost-only on the VPS, reached from elsewhere via SSH tunnel — the safer option, given an unauthenticated or network-exposed Redis/Postgres is a common breach vector).
- Added deploy/docker-compose.yml: postgres:16-alpine and redis:7-alpine, both bound to 127.0.0.1 only, both requiring a password read from .env (Postgres via POSTGRES_PASSWORD, Redis via `--requirepass`), healthchecks for both, named volumes for persistence. Validated as syntactically correct YAML with Python's yaml module (no Docker available in this dev environment to run `docker compose config` directly).
- Added deploy/.env.example (placeholder passwords only, with `openssl rand -base64 32` generation instructions) and fixed .gitignore: the existing `.env.*` pattern would have also excluded `.env.example` from being committed, so added `!.env.example` to un-ignore it specifically while the real `.env` stays ignored.
- Added scripts/verify_live_storage.py: a manual (not CI/hook-wired) round-trip check that connects to real Postgres+Redis via env-var-supplied credentials, calls `PostgresStorageBackend.ensure_schema()`, then does a real enroll->get(cache-hit)->delete cycle through `PostgresRedisStorageBackend` — this is the first thing that can actually close the "untested against live services" gap flagged repeatedly in docs/verification-checklist.md since the storage module was built, since InMemoryStorageBackend's contract tests can't exercise the real adapters at all. Fixed an import-path bug caught immediately by running it (script's own directory was on sys.path, not the repo root, so `import loomhash` failed) by adding a small sys.path shim rather than requiring `pip install -e .` first.
- Added deploy/README.md with the full flow: install Docker on the VPS, clone the repo there, configure .env, `docker compose up -d`, then from the dev machine open an SSH tunnel (`ssh -N -L 5432:127.0.0.1:5432 -L 6379:127.0.0.1:6379 ...`) and run verify_live_storage.py through it. Also documented (but did not implement) how to point `create_app()` at the real backend instead of the in-memory default, noting no deployment entrypoint wires this up yet.
- This session has no VPS access and did not run docker compose or the verify script against real services — the user needs to do that themselves and report back before docs/verification-checklist.md's ENR-05/AUT-02/REV-01 rows can be upgraded from "contract only"/"in-memory backend" to fully verified against live Postgres/Redis.
- Verification: validated docker-compose.yml's YAML syntax; ran verify_live_storage.py locally with no env vars set and confirmed it fails with a clear message instead of a traceback; re-ran the full suite (`python -m unittest discover -s tests -t .` 46/46, `python scripts/check_constraints.py` 0 violations, `python scripts/check_docs.py` consistent) to confirm nothing else broke.

## 2026-09-18 — Real Postgres/Redis verified against live services on the user's VPS

- User deployed deploy/docker-compose.yml on their VPS (via a Dokploy-style panel) and confirmed via `ss -tlnp` that both Postgres and Redis are bound to 127.0.0.1 only, not 0.0.0.0 — the loopback-only design held even through the deployment panel.
- Walked through several real deployment friction points live, none of which were code bugs: (1) the SSH tunnel command needs to run from the dev machine, not from inside an already-open VPS shell; (2) the VPS's minimal Debian image had no `python3-pip` or `redis`/`psycopg` installed, needed `apt install python3-pip` then `pip install --break-system-packages` (PEP 668 externally-managed-environment restriction on modern Debian); (3) first verify_live_storage.py run failed Postgres auth because the Docker volume had already been initialized (possibly with different/no credentials) before `.env` held the final password — Postgres only applies `POSTGRES_PASSWORD` on first init of a fresh data directory, so `docker compose down -v && docker compose up -d` was needed to reinitialize with the real password.
- Flagged to the user that they'd pasted real Postgres/Redis passwords into this chat and suggested rotating them after verification, since this is a throwaway/test setup rather than treating it as a non-issue.
- `scripts/verify_live_storage.py` run directly on the VPS (not through a tunnel — 127.0.0.1 there correctly refers to the Docker-exposed loopback ports, equally valid) passed completely: schema creation, enroll, get through the write-through cache, and delete confirmed gone from both stores, all against real Docker-hosted PostgreSQL 16 and Redis 7.
- Updated docs/verification-checklist.md: ENR-05 and AUT-02 upgraded from "contract only" (in-memory fake) to verified against both the fake and real infra; REV-01 updated to note the happy-path delete is now verified against real infra, while explicitly preserving the still-true caveat that a genuine partial-outage scenario (one store actually failing) remains untested against the real adapters.
- Updated docs/agents/storage.md's gap note to point at this resolution and restate precisely what's still not covered (real partial failure, concurrent access, cache TTL/eviction — none of which were touched by this happy-path round-trip).
- Verification: re-ran `python scripts/check_docs.py` after the edits (consistent). The actual live-infra verification evidence is the user's own terminal output, reproduced in this session's conversation, not something this session executed directly (no VPS access).

## 2026-09-18 — Major architecture revision: server-side Inference, "zero-knowledge" renamed (D-04, D-08 resolved; D-11/D-12/D-13 opened)

- User provided a detailed spec for `loom_engine_int8.onnx` (MobileNetV4 backbone + linear regression head, INT8-quantized, input `[1,3,224,224]` RGB image, output `[1,128]` vector) with explicit "Agent Directives" to integrate it server-side in FastAPI.
- Flagged before implementing anything: running this server-side means the server receives raw images, directly conflicting with the then-current constraint 4 (media processing confined to client), ENR-03 (transmit only the vector), and the project's "zero-knowledge" branding — and it makes the just-built Edge Extraction module's client-side geometric-math feature computation redundant. Declined to silently pick a side; asked where the model should actually run.
- User's clarification revealed the real product context: LoomHash is a self-hosted SDK/API-key product developers embed for biometric login in their own apps, and the user wants (a) the model weights to stay proprietary and (b) a future "evolutionary" model that adapts to how a user's face changes over time.
- Pointed out directly: client-side inference (onnxruntime-web) cannot protect model weights under any circumstance — weights must be present in cleartext on any device that runs inference, full stop, not a solvable engineering problem. This reframed the decision into a three-way tension: zero-knowledge (needs client-side) vs. protected weights (needs server-side) vs. an evolving model trained on real usage data (strongly favors server-side/centralized data). Recommended dropping strict client-side-only in favor of server-side + redefining the privacy claim, given the user's stated priorities.
- User proposed a middle path: transfer images to the server, extract the vector, purge immediately, keep zero-knowledge branding. Corrected this directly: that's a real and good privacy pattern (ephemeral/no-persistence processing) but it is **not** zero-knowledge by definition — the server does see the raw data, it just doesn't retain it. Flagged two concrete risks in "immediately purged": (1) Python/ASGI stacks copy request bodies through multiple buffers and GC timing isn't controllable, so any stray logging/APM middleware could silently defeat the claim; (2) it directly conflicts with the "evolutionary model" goal, since an immediate-purge-with-no-exception policy means no training data ever accumulates from production use.
- User confirmed both recommendations: (1) rename "zero-knowledge" to an accurate claim across the docs, (2) design a separate, explicit opt-in data-retention pipeline for future model training, distinct from the default ephemeral auth flow.
- Rewrote system.md (the approved architecture change, per the session protocol in docs/memory-system.md): project name/mission dropped "Zero-Knowledge," added an explicit "Privacy model" note defining "ephemeral, no-persistence processing" and explicitly telling future agents not to use the term "zero-knowledge"; Modular Architecture and Constraint 3/4 rewritten so the Inference module is the sole server-side component permitted to receive raw images, ephemerally only; System Architecture's Inference Engine entry marked server-side with the weight-protection rationale; Feature 1/2 pipelines rewritten so the client transmits images (post MediaPipe face-detection filtering) instead of a pre-computed vector, and the server's Inference module computes the vector; added Feature 4 (Opt-In Model-Improvement Data Pipeline) as a design-only, not-yet-implemented section with explicit "do not implement until resolved" language.
- Resolved D-08 (zero-knowledge definition) and D-04 (ONNX purpose/integration) in docs/open-decisions.md with the full reasoning chain, including the exact agreed I/O tensor contract (name/shape/dtype/normalization) for whenever a real model file exists. Flagged that server-side image processing likely raises the compliance bar (GDPR Art. 9/DPDP special-category processing) beyond what D-09's existing findings assumed — not resolved, explicitly flagged as new surface for whoever picks up compliance work.
- Opened three new decisions this creates: D-11 (Feature 4's retention/deletion/training-methodology/model-distribution details, all undefined), D-12 (API Gateway's `/v1/enroll`/`/v1/authenticate` routes still only accept `{user_id, vector}` — need reworking to carry images, encoding not decided, genuinely blocking), D-13 (disposition of `edge/feature_extraction.mjs`'s now-superseded geometric-math code — 15 passing tests, real invariance proofs, explicitly not to be deleted without a decision).
- Updated docs/requirements.md (ENR-01/02/03 rows and the Technology/Media-handling table), docs/verification-checklist.md (ENR-02/ENR-03/AUT-01 downgraded from "Verified" to "Pending (redefined)" with their prior evidence explicitly relabeled as describing the superseded design, not deleted or hidden), docs/agents/inference.md (full rewrite — module is no longer "undefined," but still has zero code pending D-12/D-13/the model artifact), docs/agents/api-gateway.md and docs/agents/edge-extraction.md (revision notices pointing at D-12/D-13, clarifying that their currently-described I/O contracts reflect live-but-superseded code, not a mistake to silently fix).
- **No application code was touched this session entry** — this was entirely an architecture/documentation resolution. loomhash/api/, loomhash/storage/, loomhash/cryptography/, loomhash/compliance/, and edge/ are all unchanged and their tests still pass (46 Python + 15 JS, re-run after all doc edits to confirm no accidental drift).
- Verification: re-ran the full test suite and both guardrail scripts after all documentation changes — all still pass, confirming the revision was documentation-only as intended. `python scripts/check_docs.py` consistent throughout each edit.

## 2026-09-18 — Stage 1 training pipeline built; dataset licensing checked; pivoted local->Colab

- User asked to train the model and produce the actual .onnx file. Before writing any code, checked the four datasets the user named (FairFace, CelebA, LFW, WIDER FACE) against their actual primary-source license terms rather than assuming they were usable for a commercial product: FairFace is CC BY 4.0 (commercial-OK, used); CelebA is explicitly "non-commercial research purposes only" and extends that restriction to derived data (blocked); WIDER FACE is CC BY-NC-ND 4.0 and has no per-identity labels at all, structurally unusable for metric learning regardless of license (blocked); LFW's license couldn't be confirmed from its primary source (DNS failure reaching vis-www.cs.umass.edu from this environment) and it's flagged in the AIAAIC AI-incident repository for consent concerns, so it was left out pending the user's own verification.
- User's chosen training strategy: start from edge/feature_extraction.mjs's existing math, then train for metric learning. Since FairFace has no identity labels, this got split into two stages: Stage 1 (distillation -- train the CNN to reproduce MediaPipe+geometric-math's output directly from pixels, no identity pairs needed, FairFace-compatible) now, Stage 2 (metric learning, needs an identity-labeled commercially-clear dataset not yet sourced) deferred.
- Ported edge/feature_extraction.mjs's math to Python (training/feature_targets.py) to generate training labels, and cross-validated it against the real JS implementation via a Node subprocess (training/test_feature_targets.py + js_cross_check.mjs) rather than trusting the port by inspection. This caught a real bug: Python's round() uses banker's rounding (round-half-to-even), JavaScript's Math.round() always rounds half up -- they disagree exactly at the two .5 boundaries the index formula hits (i=16 -> 58.5, i=80 -> 292.5), which would have silently produced 2/128 wrong label values across the entire training set. Fixed with a `_js_round` helper matching JS semantics; all 4 cross-validation tests pass.
- Checked local environment: real GPU present (NVIDIA GTX 1650, 4GB VRAM), network egress works (confirmed against huggingface.co). Attempted local training: installed a CUDA-enabled torch build (worked, confirmed via an actual GPU matmul), but a subsequent `pip install datasets` silently pulled in a newer fsspec that broke torchvision's ABI compatibility (torchvision 0.23.0 built against torch 2.8.0, we were now on 2.6.0+cu124) -- torchvision failed to import entirely. Also hit a transient MemoryError importing mediapipe's Tasks API while a large pip download ran concurrently (confirmed transient by retrying after the download finished -- not a real bug).
- User redirected mid-fix to train on Google Colab instead, avoiding further local CUDA/dependency fighting and getting a better free GPU. Left the local torch/torchvision install in its current broken state rather than spending more time fixing it -- production `loomhash` never imports torch, so this doesn't affect the actual product; only this local training experimentation was ever using it.
- Verified the label-generation pipeline for real (not just unit-tested) before building the full Colab notebook: downloaded the actual MediaPipe FaceLandmarker.task model and a real sample face image, ran Python's mediapipe.tasks.python.vision.FaceLandmarker end to end -- got a real 478-landmark detection. This also verified, for the first time against real MediaPipe output rather than assumption, that edge/mediapipe_adapter.mjs's guess about landmark ordering (468 canonical first, 10 iris appended after) was correct.
- Built training/generate_labels.py (local/offline batch label generation from a directory of images) and smoke-tested it end to end on the one real sample image -- produced a correct 128-d target vector.
- Built training/colab_train_stage1.ipynb: a complete, self-contained Colab notebook that clones the actual GitHub repo (so it trains against the exact committed, cross-validated feature_targets.py rather than a pasted-in copy), downloads FairFace via the `datasets` library, generates labels via MediaPipe entirely in-memory (no per-image disk round-trip), defines the model (timm's `mobilenetv4_conv_small` backbone -- not in torchvision, needed timm specifically -- plus a linear head to 128-d), trains with MSE loss against the distillation targets, exports to ONNX matching the exact agreed I/O contract (input "input" [1,3,224,224] float32 ImageNet-normalized, output "output" [1,128] float32), does static INT8 quantization via onnxruntime.quantization with a real calibration set, and verifies the quantized model's output shape and fp32-vs-int8 cosine similarity on a real image. Validated the notebook file itself is well-formed JSON (24 cells) before considering it done.
- Added training/requirements.txt (training-only deps: torch, torchvision, timm, mediapipe, datasets, onnx, onnxruntime) and training/README.md documenting the two-stage plan, the exact dataset-licensing findings above, and what this pipeline does NOT do (no Stage 2, no biometric accuracy claim, no wiring into the actual service).
- Updated .gitignore so training/models/ (downloaded model/test assets) and generated *.jsonl/*.pt/*.onnx files aren't committed -- the resulting loom_engine_int8.onnx gets added deliberately, in its own commit, once the user actually runs the notebook and produces it.
- **No production code touched.** Verification: re-ran the full test suite after all training/ additions -- 46 Python + 15 JS + 4 new training cross-validation tests, all pass. Both guardrail scripts still pass (no new constraint violations; the training/ scripts correctly don't trigger the server-path heuristics since they're training-time-only, not part of the deployed service).
- **Not yet done, requires the user:** actually running the Colab notebook and downloading the resulting loom_engine_int8.onnx. Once that exists, docs/open-decisions.md D-04 and docs/verification-checklist.md need updating to say a real trained artifact exists (everything currently still says it doesn't), and only then should loomhash/inference/ actually get built (still blocked on D-12 regardless).

## 2026-09-18 — Expanded Stage 1 to CelebA + WIDER FACE (non-commercial R&D scope, explicit)

- User clarified LoomHash is currently in R&D/proof-of-concept phase, not a commercial release yet, and asked to use the two previously-blocked non-commercial datasets (CelebA, WIDER FACE) for training under that framing. This is a legitimate distinction -- both licenses' restriction is specifically "non-commercial," which an internal R&D/validation phase satisfies -- but flagged the real consequence before proceeding: CelebA's license explicitly extends its restriction to *derived data*, meaning the resulting model (and anything fine-tuned from it) can only be used for this validation phase, never shipped commercially; productionizing later requires retraining from scratch on commercially-clear data, not continuing from this checkpoint.
- Checked whether CelebA has identity annotations (it does: 10,177 identities across 202,599 images, confirmed against the primary CUHK page) -- this would unlock Stage 2 (metric learning), which was the user's original goal. However, the identity file itself is explicitly "released upon request for research purposes only" directly from CUHK -- not bundled in the standard image/attribute download. A HuggingFace mirror (flwrlabs/celeba) happens to expose a `celeb_id` column, but the notebook deliberately does not use it, since the user hasn't confirmed they've gone through CUHK's actual request process and using a third-party mirror's redistribution of gated data isn't something to casually assume is fine -- same standard applied to the earlier LFW caution.
- Asked how to proceed given that gap; user chose to expand Stage 1 with CelebA+WIDER FACE now (images only) and leave Stage 2 deferred until the identity file is actually obtained.
- Updated training/colab_train_stage1.ipynb: added CelebA (`flwrlabs/celeba`, images only) and WIDER FACE (`CUHK-CSE/wider_face`) as additional Stage 1 data sources alongside FairFace. WIDER FACE required extra handling since it's a face-*detection* dataset (cluttered scenes with bounding boxes, not single-face portrait crops like FairFace/CelebA): added a padded bounding-box crop step, plus filtering out invalid/heavily-occluded/heavily-blurred/too-small faces before they ever reach MediaPipe, since WIDER FACE is deliberately full of hard detection cases that would mostly just fail label generation or add noise to a regression-distillation target.
- Verified dataset schemas before writing code against them (not guessed): fetched CelebA's mirror schema (confirmed `image`/`celeb_id`/40 attribute columns, license field pointing back to the original CUHK agreement) and WIDER FACE's exact schema (`image` + `faces` dict with `bbox` in COCO x/y/w/h format, plus `blur`/`occlusion`/`invalid` quality fields) via WebFetch before use.
- Updated training/README.md: reframed the whole dataset-licensing table around the explicit "non-commercial R&D scope only" decision, added a prominent scope-boundary section (derived-data restriction, no continuing this checkpoint into a commercial build), and updated the Stage 2 status to reflect that CelebA's identity file is obtainable in principle but not yet obtained.
- Re-validated the updated notebook is well-formed JSON (still 24 cells) before considering it done. No production code touched; guardrail scripts and doc-consistency check re-run clean.

## 2026-09-18 — Two real bugs hit during the user's first actual Colab run, both fixed

- User ran the notebook for real (not something this session could do itself -- no Colab access). Two genuine bugs surfaced, both fixed in the repo and reported back to the user with an immediate in-session workaround so their running notebook didn't have to restart from scratch:
  1. `load_dataset('HuggingFaceM4/FairFace', split='train')` failed immediately: `ValueError: Config name is missing. Please pick one among the available configs: ['0.25', '1.25']`. Fixed by adding the config name; picked `'0.25'` (the tighter face-crop padding variant) over `'1.25'` (much more background/shoulders included).
  2. After FairFace and CelebA succeeded (30,000 images), WIDER FACE failed: `RuntimeError: Dataset scripts are no longer supported, but found wider_face.py`. Investigated rather than guessing at a fix: `CUHK-CSE/wider_face` uses a legacy "loading script" HF repo format that the `datasets` library has dropped support for outright, and confirmed via the datasets-server API that HF's own dataset viewer refuses to auto-convert it to Parquet ("runs arbitrary Python code"). No drop-in replacement mirror was found. Rather than chase a fragile Google-Drive-direct-download fix for what was already the highest-effort, lowest-marginal-value dataset (no identity labels either way, heavily filtered), wrapped the WIDER FACE block in a try/except that logs a warning and continues with whatever FairFace+CelebA data is already pooled -- the run is no longer blocked by this dataset being unavailable.
  - Told the user the immediate unblock for their live session (skip the cell, continue -- FairFace+CelebA were already loaded) separately from the permanent repo fix, since they couldn't wait for a new notebook file mid-run.
  - Updated training/README.md's dataset table to mark WIDER FACE "optional, currently unavailable via `datasets`" with the specific error and investigation findings, rather than silently downgrading it.
  - Validated the notebook JSON after each fix; re-ran `python scripts/check_docs.py` clean.

## 2026-09-18 Session 2

- Implemented Inference module with onnxruntime and loom_engine_int8.onnx
- Rewrote API Gateway routes to accept multipart/form-data (D-12)
- Wrote test suites for inference module and new multipart routes (59/59 passing)
- Updated edge JS documentation to mark geometric feature extraction as superseded (D-13)
- Updated documentation: open-decisions.md, agents/inference.md, agents/api-gateway.md, verification-checklist.md

## 2026-09-20 — Local macOS dev environment configured (Python 3.11 + Node v20), full test suite verified

- Initial state on local Mac: system was on Xcode Python 3.9.6 (violating Constraint 2 floor of >=3.10) with no project dependencies installed, and no Node.js available for edge/ JS tests.
- Installed `uv` and provisioned managed standalone CPython 3.11.16 (`~/.local/share/uv/python/`).
- Created local `.venv` and installed all project and test dependencies (`numpy`, `psycopg[binary]`, `redis`, `fastapi`, `uvicorn`, `python-multipart`, `onnxruntime`, `Pillow`, `httpx`).
- Installed Node.js v20.18.0 standalone in `~/.local/` and linked `python3`, `python3.11`, `node`, `npm`, `npx` into `~/.local/bin` (sourced in `~/.zshrc`).
- Configured git pre-commit hook execution: marked `hooks/pre-commit` executable and configured `git config core.hooksPath hooks`.
- Added `*.egg-info/` to `.gitignore`.
- Verification:
  - `python scripts/check_constraints.py`: 0 hard violations under Python 3.11.
  - `python scripts/check_docs.py`: memory files present and internally consistent.
  - `.venv/bin/python -m unittest discover -s tests -t . -v`: 56/56 Python tests pass.
  - `node --test edge/`: 15/15 JavaScript tests pass.
  - `.venv/bin/python training/test_feature_targets.py`: 4/4 cross-validation tests pass.

