# Verification checklist

Living document. One row per functional requirement from [requirements.md](requirements.md). A status changes only when real evidence exists — a test run, a reviewed fixture, a reproducible check — not because code was written. See [memory-system.md](memory-system.md) for the update protocol.

Status values: **Pending** (not yet implemented or not yet tested), **Verified** (evidence exists and is linked), **Blocked** (cannot be verified until a listed open decision is resolved).

| ID | Requirement | Status | Blocked by | Evidence |
| --- | --- | --- | --- | --- |
| ENR-01 | Capture 12 views across a 1s yaw rotation | Implemented, untested in a real browser | — | `captureEnrollmentFrames()` in [edge/applet/main.mjs](../edge/applet/main.mjs) runs `ENROLLMENT_FRAME_COUNT` (12) detections spaced evenly across ~1000ms. No browser/webcam is available in the development environment, so this has never actually run against a live camera — see [edge/README.md](../edge/README.md) for how to run the manual test applet and move this to Verified. |
| ENR-02 | Compute mean 3D landmarks -> 128-d rigid-distance vector | Verified (algorithmic only, reinterpreted) | — | Implemented as "derive the 128-d vector per frame, then average the resulting vectors" rather than the literal "average landmarks, then derive one vector" — see D-02's resolution in [open-decisions.md](open-decisions.md) for why. `extractFeatureVector`/`computeEnrollmentVector` in [edge/feature_extraction.mjs](../edge/feature_extraction.mjs)/[edge/capture.mjs](../edge/capture.mjs), unit tested (including proofs of rotation/translation/scale invariance) in `edge/*.test.mjs` via `node --test edge/` — 15/15 pass. Tested only against synthetic landmark arrays, never real MediaPipe output; biometric-accuracy evaluation remains separately required. |
| ENR-03 | Transmit only the numerical vector | Verified (server-side enforcement only) | — | `EnrollRequest`/`AuthenticateRequest` in [loomhash/api/schemas.py](../loomhash/api/schemas.py) use `extra="forbid"` and a fixed `vector: list[float]` field, so the server rejects (422) any request carrying an unexpected field (e.g. an `image` field) or a wrong-length vector; tested in `test_enroll_rejects_unexpected_extra_field`/`test_enroll_rejects_wrong_length_vector` in [tests/test_api/test_routes.py](../tests/test_api/test_routes.py). This verifies the server can't be handed anything but a vector — it does not verify the client (Edge Extraction, not yet built) never attempts to send more. |
| ENR-04 | Random 32-byte seed + seeded LSH -> 256-bit loom_hash | Verified (algorithmic only) | — | `generate_seed`/`project` in [loomhash/cryptography/lsh.py](../loomhash/cryptography/lsh.py), tested in [tests/test_cryptography/test_lsh.py](../tests/test_cryptography/test_lsh.py) (determinism, seed/vector sensitivity, range, input validation) against synthetic vectors. Biometric-accuracy evaluation is separately required per the proposed validation sequence below before this can be called biometrically verified. |
| ENR-05 | Persist (user_id, seed, loom_hash) | Verified — contract (in-memory fake) and against real infra | — | `InMemoryStorageBackend` contract tested in [tests/test_storage/test_contract.py](../tests/test_storage/test_contract.py) (round-trip, upsert-on-re-enroll, per-user isolation). `PostgresRedisStorageBackend` run against real Docker-hosted PostgreSQL 16 + Redis 7 on 2026-09-18 via [scripts/verify_live_storage.py](../scripts/verify_live_storage.py) (see [deploy/](../deploy/)) — schema creation, enroll, and get all confirmed working against live services, not just the in-memory fake. |
| AUT-01 | Single-frame capture -> same 128-d feature representation | Verified (algorithmic only) | — | `computeAuthenticationVector()` calls the identical `extractFeatureVector()` used per-frame during enrollment (N=1, no averaging) — tested in `edge/capture.test.mjs::authentication vector matches extractFeatureVector directly`. Comparability with real enrollment data is untested against a live camera (same caveat as ENR-01/ENR-02). |
| AUT-02 | Retrieve stored seed/template, project query with stored seed | Verified — contract (in-memory fake) and against real infra | — | Same evidence as ENR-05 — `get()`'s write-through-cache read path (Redis hit after a Postgres write) confirmed working against real Postgres/Redis on 2026-09-18. |
| AUT-03 | `(stored_hash ^ query_hash).bit_count() / 256.0` | Verified | — | `hamming_distance` in [loomhash/cryptography/matching.py](../loomhash/cryptography/matching.py), tested in [tests/test_cryptography/test_matching.py](../tests/test_cryptography/test_matching.py) against known bit-pattern hashes (identical, fully-different, 51-bit-difference cases). |
| AUT-04 | 200 if distance <= 0.20 else 401 | Verified end-to-end (in-memory backend) | — | `POST /v1/authenticate` in [loomhash/api/app.py](../loomhash/api/app.py) returns 200/401 via a real HTTP request through the full enroll->authenticate loop (`TestClient`, in-memory storage), tested in [tests/test_api/test_routes.py](../tests/test_api/test_routes.py) (`test_authenticate_with_same_vector_succeeds`, `test_authenticate_with_different_vector_fails`, `test_authenticate_unknown_user_returns_401_not_404`). Not yet run against the real Postgres/Redis backend (see ENR-05/AUT-02) or against real biometric vectors. |
| REV-01 | Delete user's seed from PostgreSQL and Redis on revocation | Verified end-to-end (in-memory + API); happy path also verified against real infra | — | `DELETE /v1/users/{user_id}` -> `loomhash.compliance.revoke()` -> `storage.delete()`, tested in `test_routes.py` and `tests/test_compliance/test_revocation.py`. The partial-failure *wiring* is tested (`test_revoke_returns_500_when_storage_reports_incomplete_deletion`) via a stub backend whose `delete()` always returns False, proving the API/Compliance layer surfaces a failure as 500, not success. Separately, `PostgresRedisStorageBackend.delete()`'s happy path (both stores actually delete, confirmed gone from both) was run against real Docker-hosted Postgres/Redis on 2026-09-18 via `scripts/verify_live_storage.py` — see ENR-05. **Still not tested:** a real partial-outage scenario (e.g. Redis reachable but Postgres down mid-delete) against the actual adapters — the stub proves the call chain reacts correctly to *a reported* failure, not that the real adapter correctly detects and reports failure when one live service genuinely fails while the other succeeds. |

## How to fill in evidence

- For a deterministic/algorithmic requirement (e.g., AUT-03's boundary math), evidence is a link or path to the test file plus the specific cases run (e.g., "51 differing bits -> accept, 52 -> reject, tested in `tests/test_matching.py::test_boundary`").
- For a requirement involving real biometric input (e.g., ENR-01/ENR-02), evidence must reference the consented evaluation protocol described in [open-decisions.md](open-decisions.md)'s proposed validation sequence — not a single developer's own face tested once.
- For a storage/lifecycle requirement (e.g., ENR-05, REV-01), evidence is the specific adapter test plus which failure modes (partial write, concurrent access) were exercised.
- If a row is marked Verified, the evidence field must be specific enough for another agent to re-run or re-check it without asking the original author.

## Current state

Every row is Pending: no application code exists yet (see STATUS.md). This table exists now so that as soon as implementation starts, "done" means "verified here," not "code was written."
